<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dunn.instrument">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.SYSTEM_OVERLAY_WINDOW" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:name=".MainApp"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".SecondActivity"/>

        <service
            android:name=".service.DeviceInfoService">
        </service>
        <service
            android:name=".service.InfoService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.coocaa.perf.action" />
            </intent-filter>
        </service>
        <service
            android:name=".service.InfoTest1Service">
        </service>
        <service
            android:name=".service.InfoTest2Service">
        </service>
        <service
            android:name=".service.InfoTest3Service">
        </service>
        <service
            android:name=".service.InfoTest4Service">
        </service>
        <service
            android:name=".service.InfoTest5Service">
        </service>
        <service
            android:name=".service.InfoTest6Service">
        </service>
    </application>

</manifest>